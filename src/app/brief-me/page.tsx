type Params={q?:string|string[]};
export default async function BriefMe({searchParams}:{searchParams:Promise<Params>}){
  const params=await searchParams;
  const raw=Array.isArray(params.q)?params.q[0]:params.q;
  const q=(raw||"").trim().slice(0,200);
  return <main className="shell"><section className="hero"><p className="eyebrow">BRIEF ME · V3</p><h1>{q?<>Brief me on <em>{q}</em>.</>:<>What should we brief you on?</>}</h1><p className="lede">Briefs resolves the subject into entities, current claims, meaningful changes, evidence, and missing knowledge before composing an answer. If evidence is insufficient, the correct result is research needed — not invention.</p><form action="/brief-me" className="brief-form"><input aria-label="Brief subject" name="q" defaultValue={q} placeholder="Nvidia, AI agents, private credit…" autoComplete="off"/><button type="submit">Brief me</button></form><div className="chips"><span>30 sec</span><span>3 min</span><span>Deep dive</span><span>Primary sources</span></div></section><section className="grid"><article className="card"><p className="eyebrow">FAST PATH</p><h2>Existing knowledge first</h2><p>Entity resolution → verified claims → recent changes → importance → Brief Composer.</p></article><article className="card"><p className="eyebrow">RESEARCH PATH</p><h2>Know when evidence is missing</h2><p>V3 marks researchNeeded instead of inventing an answer when the knowledge graph is incomplete.</p></article><article className="card"><p className="eyebrow">TIME-AWARE</p><h2>Ask what changed</h2><p>The Brief Request supports time windows so later versions can answer “since January” or “since I was here.”</p></article></section></main>;
}
