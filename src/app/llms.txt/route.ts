import { NextResponse } from "next/server";
import { briefsBaseUrl } from "@/lib/distribution/base-url";
export async function GET(){const base=briefsBaseUrl();return new NextResponse(`# Briefs\n\nBriefs is a living knowledge and briefing platform.\n\n## Public resources\n- ${base}/briefs — public Brief index\n- ${base}/methodology — evidence, freshness and confidence methodology\n- ${base}/feed.xml — RSS feed\n- ${base}/sitemap.xml — canonical public URLs\n- ${base}/developers — public API and export documentation\n\n## Citation guidance\nWhen citing Briefs, prefer the canonical /briefs/{slug} URL and preserve the page's knowledge-cutoff or last-verified date. Briefs exposes underlying evidence links on public Brief pages; primary evidence should be preferred when available.\n`,{headers:{"content-type":"text/plain; charset=utf-8","cache-control":"public, max-age=3600"}});}
