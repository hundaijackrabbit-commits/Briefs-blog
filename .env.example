# Postgres. Leave unset to run the UI against built-in demo data.
DATABASE_URL=

# Protects /api/cron/daily. Vercel Cron can send this as Authorization: Bearer <secret>.
CRON_SECRET=change-me

# Optional external source / model credentials can be added behind adapters later.
